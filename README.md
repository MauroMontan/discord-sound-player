# Discord bot for playing sounds



> You need ffmpeg to use this bot.

- if you are in Debian-based distributions.

```
sudo apt install ffmpeg
```

then install python dependencies:

```
pip install -r requirements.txt
```

